{
  "name": "postcss-rows",
  "version": "0.0.2",
  "description": "Using the rows unit to represent vertical spacing from a design document",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Andrew JD Hudson",
  "license": "ISC",
  "devDependencies": {
    "postcss": "^4.1.13",
  }
}
